# uploads

Drop the CV PDF here as `CerriG_Resume.pdf` — that is the filename the
**Download CV** button in `index.html` links to.

It could not be copied over from the Claude Design project automatically: the
project's file-read API caps responses at 256 KiB and both PDFs there
(`CerriG_Resume.pdf` and `Resume_GC.pdf`) exceed that, so only a truncated,
unopenable fragment came back. Download it from the project directly instead.
